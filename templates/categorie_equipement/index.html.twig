{% extends ('basefront/base.html.twig') %}
{% block body %}
<!DOCTYPE html>

<title>CategorieEquipement index</title>

    <h1 class="section-title" align="center">Nos Categories</h1>
<h4 class="sub-title"  align="center">Chaque categorie contient des equipements ! !</h4>
<table align="center">
{% for categorie_equipement in categorie_equipements %}
    <td>
<div  class="card text-white bg-dark mb-3"style="width: 18rem;" align="center">
  
  <div class="card-body" >
      <h5 class="card-title">{{ categorie_equipement.nomCategorieEquipement }}</h5>
      
                
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        <div/>
        <div/>
        <td/>
    </table>
{% endblock %}
