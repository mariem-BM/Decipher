{% extends ('baseback/base.html.twig') %}
{% block title %}Billet{% endblock %}


{% block body %}
    <h1>Billet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ billet.id }}</td>
            </tr>
            <tr>
                <th>Chair_billet</th>
                <td>{{ billet.chairBillet }}</td>
            </tr>
            <tr>
                <th>Voyage_num</th>
                <td>{{ billet.voyageNum }}</td>
            </tr>
            <tr>
                <th>Terminal</th>
                <td>{{ billet.terminal }}</td>
            </tr>
            <tr>
                <th>Portail</th>
                <td>{{ billet.portail }}</td>
            </tr>
            <tr>
                <th>Embarquement</th>
                <td>{{ billet.embarquement ? billet.embarquement|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('billet_index') }}">back to list</a>

    <a href="{{ path('billet_edit', {'id': billet.id}) }}">edit</a>

    {{ include('billet/_delete_form.html.twig') }}
{% endblock %}
