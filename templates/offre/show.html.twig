{% extends ('base_back_f/basebackF.html.twig') %}


{% block title %}Offre
{% endblock %}

{% block body %}
<h1>Offre</h1>

<table class="table">
	<tbody>
		<tr>
			<th>Id</th>
			<td>{{ offre.id }}</td>
		</tr>
		<tr>
			<th>Nom_offre</th>
			<td>{{ offre.nomOffre }}</td>
		</tr>
		<tr>
			<th>Description_offre</th>
			<td>{{ offre.descriptionOffre }}</td>
		</tr>
		<tr>
			<th>Prix_offre</th>
			<td>{{ offre.prixOffre }}</td>
		</tr>
		<tr>
			<th>Duree_offre</th>
			<td>{{ offre.dureeOffre }}</td>
		</tr>
	</tbody>
</table>
<div>
	<a href="{{ path('offre_index') }}" class="btn btn-primary">back to list</a>

	<a href="{{ path('offre_edit', {'id': offre.id}) }}" class="btn btn-primary">edit</a>

	<a {{ include('offre/_delete_form.html.twig') }} </a> <div> {% endblock %}
