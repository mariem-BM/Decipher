{% extends ('base_back_f/basebackF.html.twig') %}


{% block title %}Reclamation
{% endblock %}

{% block body %}
	<h1>Reclamation</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ reclamation.id }}</td>
			</tr>
			<tr>
				<th>Description_reclamation</th>
				<td>{{ reclamation.descriptionReclamation }}</td>
			</tr>
			<tr>
				<th>Date_reclamation</th>
				<td>{{ reclamation.dateReclamation ? reclamation.dateReclamation|date('Y-m-d') : '' }}</td>
			</tr>
	<tr>
				<th>User</th>
				<td>{{ reclamation.user }}</td>
			</tr>
		</tbody>
	</table>
	<div>
		<a href="{{ path('reclamation_index') }}" class="btn btn-primary">back to list</a>

		

	</div>
{% endblock %}
