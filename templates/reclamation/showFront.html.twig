{% extends ('basefront/base.html.twig') %}


{% block title %}Reclamation
{% endblock %}

{% block body %}
	<h1>Reclamation</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ reclamation.id }}</td>
			</tr>
			<tr>
				<th>Description_reclamation</th>
				<td>{{ reclamation.descriptionReclamation }}</td>
			</tr>
			{#<tr>
		<th>Date_reclamation</th>
				<td>{{ reclamation.dateReclamation ? reclamation.dateReclamation|date('Y-m-d') : '' }}</td>
			</tr>
#}
		</tbody>
	</table>
	<div>
		<a href="{{ path('reclamation_indexreclamFront') }}" class="btn btn-primary">back to list</a>

	
	{{ include('reclamation/_delete_form.html.twig') }}	 


	</div>
{% endblock %}
